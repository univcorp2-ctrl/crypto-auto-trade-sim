import { describe, expect, it } from 'vitest'; import { calculateMaxDrawdown, calculateReturnPct } from '../src/portfolio'; describe('portfolio helpers', function() { it('calculates return', function() { expect(calculateReturnPct(110, 100)).toBeCloseTo(0.1); }); it('calculates max drawdown', function() { const history = [{ valueJpy: 100 }, { valueJpy: 120 }, { valueJpy: 90 }]; expect(calculateMaxDrawdown(history)).toBeCloseTo(-0.25); }); });