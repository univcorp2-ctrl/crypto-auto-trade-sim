export function calculateReturnPct(currentValue, initialValue) { return Number.isFinite(initialValue) && initialValue > 0 ? (currentValue - initialValue) / initialValue : 0; } export function calculateMaxDrawdown(points) { let peak = 0; let maxDrawdown = 0; for (const point of points) { peak = Math.max(peak, point.valueJpy); if (peak > 0) maxDrawdown = Math.min(maxDrawdown, (point.valueJpy - peak) / peak); } return maxDrawdown; }